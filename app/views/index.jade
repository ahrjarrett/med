include ./includes/layout
include ./mixins/mixins
body
	block content
		+fullnav(url)
		include ./includes/hero
		+infoblock(info, 'accent')
		+wordsandheads(quotes)
		+infoblock(info1, 'main')
		include ./includes/logos-alt
		+footerbg('black')
